CREATE TABLE mod_23(
	id_op_fk int(5) not null,
    p_iva_fk int(20) not null,
    man varchar(15) not null,
    rif varchar(20) not null,
    operazioni text(150),
    attrezzature text(150),
    durata int(2) not null,
    n_operai int(2) not null,
    materiali text(150),
    sodd boolean,
    altro text(150),
    
    PRIMARY KEY(id_op_fk),
    
    FOREIGN KEY(p_iva_fk) REFERENCES ditta_esterna(p_iva)
    on UPDATE CASCADE
    on delete restrict,
    
    FOREIGN KEY(id_op_fk) REFERENCES op(id_op)
    on update CASCADE
    on delete restrict
    
    
);